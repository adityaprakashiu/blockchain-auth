{
  "name": "blockchain-auth",
  "version": "1.0.0",
  "scripts": {
    "test": "npx hardhat test",
    "start": "concurrently \"npx hardhat node\" \"react-scripts start\"",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost"
  },
  "dependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "hardhat": "^2.22.19"
  }
}
